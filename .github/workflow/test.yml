name: Check Files

on:
  push:

jobs:
  check-files:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show HTML file
        run: cat Day30.html

      - name: Show CSS file
        run: cat Day30.style.css

      - name: Show JavaScript file
        run: cat Day30script.js